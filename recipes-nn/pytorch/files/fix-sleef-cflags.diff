--- ./third_party/sleef/Configure.cmake.orig	2020-08-29 15:41:02.913525181 -0400
+++ ./third_party/sleef/Configure.cmake	2020-08-29 15:40:57.958590607 -0400
@@ -334,7 +334,7 @@
   set(FLAGS_NO_ERRNO "")
 endif()
 
-set(SLEEF_C_FLAGS "${FLAGS_WALL} ${FLAGS_STRICTMATH} ${FLAGS_NO_ERRNO}")
+set(SLEEF_C_FLAGS "${CMAKE_C_FLAGS} ${FLAGS_WALL} ${FLAGS_STRICTMATH} ${FLAGS_NO_ERRNO}")
 if(CMAKE_C_COMPILER_ID MATCHES "GNU" AND CMAKE_C_COMPILER_VERSION VERSION_GREATER 6.99)
   set(DFT_C_FLAGS "${FLAGS_WALL}")
 else()
